export const environment = {
  production: true,
  // TODO: apuntar a un backend real cuando exista un hosting para FastAPI
  // (Cloudflare Pages solo sirve el frontend estatico).
  apiUrl: 'http://localhost:8000'
};
